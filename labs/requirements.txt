# Lab environment beyond the preinstalled torch (Colab 2026.07 ships torch 2.11).
# Never add torch here — labs use the platform's build (see docs/design.md §12).
torch_geometric==2.8.0.post1
matplotlib
